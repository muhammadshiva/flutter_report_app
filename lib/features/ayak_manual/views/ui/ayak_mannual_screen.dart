import 'package:flutter/material.dart';

class AyakManualScreen extends StatelessWidget {
  const AyakManualScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Bahan Baku Screen'),
    );
  }
}
