import 'package:flutter/material.dart';

class BriketScreen extends StatelessWidget {
  const BriketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Briket Screen'),
    );
  }
}
